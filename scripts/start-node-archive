#!/usr/bin/env bash

set -eux

CACHE="${1:-8192}"

geth \
    --config ./config.toml \
    --datadir ./node \
    --pprof \
    --pprofaddr 0.0.0.0 \
    --rcp \
    --metrics \
    --cache "$CACHE" \
    --gcmode archive \
    --syncmode full
