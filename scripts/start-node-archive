#!/usr/bin/env bash

set -eux

SYNCMODE="${1:-fast}"

geth \
    --config ./config.toml \
    --datadir ./node \
    --pprof \
    --pprofaddr 0.0.0.0 \
    --metrics \
    --rpc \
    --cache 8192 \
    --rpcaddr 0.0.0.0 \
    --gcmode archive
